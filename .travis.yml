language: python
python:
  - "3.6"
services:
  # - mysql
before_script:
  # - mysql -e 'CREATE DATABASE travisdb;'
install:
  - python setup.py install -q
script:
  #- pylint --load-plugins pylint_django officemanager/department/*
  - python officemanager/manage.py test
  - python officemanager/manage.py makemigrations
  - python officemanager/manage.py showemigrations
  - python officemanager/manage.py migrate
  - python officemanager/manage.py showemigrations
# this command exited with error. No file to run: 'test'
#  - coverage run --source=officemanager/manage.py test
after_success:
  - coveralls
notifications:
  email:
    recipients:
      - dzdimati@gmail.com
    on_succes: change